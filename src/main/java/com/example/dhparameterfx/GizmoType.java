package com.example.dhparameterfx;

public enum GizmoType {
    
    DRAG_A,     // Moving along X (Red)
    DRAG_D,     // Moving along Z (Blue)
    ROTATE_THETA, // Rotating around Z (Gold Hub)
    ROTATE_ALPHA  // Twisting around X (Green)
    
}
